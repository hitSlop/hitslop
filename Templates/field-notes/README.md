# Field Notes

A Svelte 5 mini app authored in the shared template workspace.

- `source/` is the human- and AI-editable app.
- `data.json` is the authoritative notebook state.

Preview with `slop dev Templates/field-notes`. Release with `slop package-templates`.
