# Random Picker

A horizontal capsule utility for editing options, making a lively pick, and retaining short history.

Preview with `slop dev Templates/random-picker`. Release with `slop package-templates`.
