# Minimal Invoice

A deliberately quiet, professional invoice with editable parties, line items,
dates, tax, and status.

Preview with `slop dev Templates/invoice-minimal`. Release with
`slop package-templates`.
