{
  "name": "hitslop",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "apps/firebase",
    "apps/landing",
    "packages/*",
    "examples/slops",
    "Prototypes/*"
  ],
  "scripts": {
    "build": "bun run schema:generate && bun run --cwd packages/schema build && bun run --cwd packages/runtime build && bun run --cwd packages/svelte build && bun run --cwd packages/cli build && bun run --cwd apps/firebase build && bun run --cwd apps/landing build && bun run --cwd examples/slops build",
    "check": "bun run schema:check && bun run --filter '@hitslop/*' check",
    "test": "bun run --cwd packages/schema test && bun run --cwd packages/runtime test && bun run --cwd packages/svelte test && bun run --cwd packages/cli test && bun run --cwd apps/firebase test && bun run --cwd examples/slops test",
    "release:check": "bun scripts/release-check.ts",
    "release:npm:check": "bun scripts/npm-release-check.ts",
    "examples:check": "bun run --cwd examples/slops check && bun run --cwd examples/slops test && bun run --cwd examples/slops build",
    "schema:generate": "bun run --cwd packages/schema generate",
    "schema:check": "bun run --cwd packages/schema check:generated",
    "slop": "bun packages/cli/src/cli.ts",
    "slops:dev": "bun run --cwd examples/slops dev",
    "firebase:dev": "bun run --cwd apps/firebase dev",
    "firebase:deploy": "bun run --cwd apps/firebase deploy",
    "landing:dev": "bun run --cwd apps/landing dev",
    "swift:build": "swift build --package-path apps/apple/Packages/HitSlopApple --product hitslop-native",
    "slops:review": "bun run --cwd examples/slops dev --review"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  },
  "patchedDependencies": {
    "@sveltejs/package@2.5.8": "patches/@sveltejs%2Fpackage@2.5.8.patch"
  }
}
