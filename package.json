{
  "name": "hitslop",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "apps/catalog",
    "apps/landing",
    "apps/registry",
    "packages/*",
    "examples/slops"
  ],
  "scripts": {
    "build": "bun run schema:generate && bun run --cwd packages/schema build && bun run --cwd packages/runtime build && bun run --cwd packages/svelte build && bun run --cwd packages/react build && bun run --cwd packages/cli build && bun run --cwd apps/registry build && bun run --cwd apps/catalog build && bun run --cwd examples/slops build",
    "check": "bun run schema:check && bun run --filter '@hitslop/*' check",
    "test": "bun run --cwd packages/schema test && bun run --cwd packages/runtime test && bun run --cwd packages/svelte test && bun run --cwd packages/react test && bun run --cwd packages/cli test && bun run --cwd apps/registry test && bun run --cwd apps/catalog test && bun run --cwd examples/slops test",
    "release:check": "bun scripts/release-check.ts",
    "schema:generate": "bun run --cwd packages/schema generate",
    "schema:check": "bun run --cwd packages/schema check:generated",
    "slop": "bun packages/cli/src/cli.ts",
    "catalog:dev": "bun run --cwd apps/catalog dev",
    "landing:dev": "bun run --cwd apps/landing dev",
    "convex:dev": "bun run --cwd apps/registry dev",
    "swift:build": "swift build --package-path apps/apple/Packages/HitSlopApple --product hitslop-native"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  }
}
