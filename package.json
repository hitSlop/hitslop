{
  "name": "hitslop",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "apps/cloudflare",
    "apps/landing",
    "packages/*",
    "examples/slops"
  ],
  "scripts": {
    "build": "bun run schema:generate && bun run --cwd packages/schema build && bun run --cwd packages/api build && bun run --cwd packages/runtime build && bun run --cwd packages/svelte build && bun run --cwd packages/cli build && bun run --cwd apps/landing build && bun run --cwd examples/slops build",
    "check": "bun run schema:check && bun run --cwd packages/api build && bun run --filter '@hitslop/*' check",
    "test": "bun run --cwd packages/schema test && bun run --cwd packages/api test && bun run --cwd packages/runtime test && bun run --cwd packages/svelte test && bun run --cwd packages/cli test && bun run --cwd apps/cloudflare test && bun run --cwd examples/slops test",
    "release:check": "bun scripts/release-check.ts",
    "release:npm:check": "bun scripts/npm-release-check.ts",
    "examples:check": "bun run --cwd examples/slops check && bun run --cwd examples/slops test && bun run --cwd examples/slops build",
    "schema:generate": "bun run --cwd packages/schema generate && bun run --cwd packages/api generate",
    "schema:check": "bun run --cwd packages/schema check:generated",
    "slop": "bun packages/cli/src/cli.ts",
    "slops:dev": "bun run --cwd examples/slops dev",
    "cloudflare:dev": "bun run --cwd apps/cloudflare dev",
    "cloudflare:deploy": "bun run --cwd apps/cloudflare deploy",
    "landing:dev": "bun run --cwd apps/landing dev",
    "swift:build": "swift build --package-path apps/apple/Packages/HitSlopApple --product hitslop-native",
    "slops:review": "bun run --cwd examples/slops dev --review",
    "test:local": "bun scripts/local-check.ts",
    "format": "prettier --write \"{packages,apps/cloudflare,apps/landing,examples/slops,scripts}/**/*.{ts,tsx,js,mjs,cjs}\"",
    "format:check": "prettier --check \"{packages,apps/cloudflare,apps/landing,examples/slops,scripts}/**/*.{ts,tsx,js,mjs,cjs}\""
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "3.9.6",
    "typescript": "^7.0.2"
  },
  "patchedDependencies": {
    "@sveltejs/package@2.5.8": "patches/@sveltejs%2Fpackage@2.5.8.patch"
  }
}
