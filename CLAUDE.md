# hitSlop

Read [skills/hitslop/SKILL.md](skills/hitslop/SKILL.md) before touching a `.slop` file.

A `.slop` is a package containing `document.sqlite`. Stat (`slop_docs` + `.schema`), then `SELECT` / `UPDATE`. The open window shares that file. Do not add per-document MCP tools.
