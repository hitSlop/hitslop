# Public Convex URLs are generated by `bunx convex dev` in apps/registry.
VITE_CONVEX_URL=
CONVEX_SITE_URL=

# Never commit the value. Configure the same secret in Wrangler and Convex.
HITSLOP_INTERNAL_SECRET=
