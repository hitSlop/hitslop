# SQLite Field Notes

A Svelte 5 mini app whose relational memory lives beside it in `data.sqlite`.

- `source/` is the human- and AI-editable app.
- `build/index.html` is the disposable Vite cartridge.
- Inspect or edit the store with `sqlite3 data.sqlite`.

Rebuild with the installed `slop build` command.
