# Field Notes

A Svelte 5 mini app whose editable source and data live beside it.

- `source/` is the human- and AI-editable app.
- `build/index.html` is the disposable Vite cartridge.
- `data.json` is the authoritative notebook state.

Rebuild with `slop build`. `theme.css` hot-reloads without a rebuild.
