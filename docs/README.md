# Documentation

The current v1 contracts are [architecture](architecture.md), [storage](storage.md), [runtime versioning](versioning.md), [package format](package-format.md), [authoring](authoring.md), and [capture](capture.md). The existing macOS client remains active, including its local catalog, frameless windows/hover toolbar, TCA, Firebase Analytics/Crashlytics and Swift CLI. Historical command/snapshot and sync documents are reference-only; hosted catalog loading, OpenAPI/Registry, accounts/Auth/App Check, and sharing are deferred.

[CLI commands, helper discovery, retries, and export](cli.md).
