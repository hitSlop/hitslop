# Self-hosting is deferred

The local macOS release needs no document server. The Cloudflare command/snapshot implementation is historical scaffolding and is not a supported deployment path. Do not deploy it as the v1 backend.

Public authoring and a hosted template catalog are planned separately. See [sharing-later](sharing-later.md). Local build/register and document editing are described in [CLI](cli.md).
