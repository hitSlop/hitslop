# Swift and Svelte

The current contract is [Document architecture](sync-v1.md).

There is one document engine and one current disk format. Quick Checklist is the
active example; deferred template sources are not supported platform clients.
