{
  "name": "@hitslop/landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "preview": "astro preview",
    "deploy": "bun run build && bun run deploy:built",
    "deploy:built": "wrangler deploy",
    "preview:upload": "wrangler versions upload"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.42.0",
    "@astrojs/svelte": "^9.0.1",
    "@hitslop/schema": "0.3.0",
    "astro": "^7.3.2",
    "svelte": "^5.57.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3",
    "wrangler": "^4.130.0"
  }
}
