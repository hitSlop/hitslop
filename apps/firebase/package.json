{
  "name": "@hitslop/firebase",
  "private": true,
  "license": "MIT",
  "version": "0.1.0",
  "type": "module",
  "main": "lib/index.cjs",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && bun build src/index.ts --target=node --format=cjs --outfile=lib/index.cjs --external firebase-admin --external firebase-functions",
    "gcp-build": "",
    "check": "tsc -p tsconfig.json",
    "test": "bun test tests",
    "test:emulator": "firebase emulators:exec --only firestore --project hitslopapp 'bun test tests/backend-emulator.test.ts && bun test tests/rules-emulator.test.ts'",
    "dev": "firebase emulators:start --project hitslopapp",
    "deploy": "FUNCTIONS_DISCOVERY_TIMEOUT=60 firebase deploy --project hitslopapp"
  },
  "dependencies": {
    "@hitslop/schema": "0.3.0",
    "fast-png": "^8.0.0",
    "fflate": "^0.8.3",
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2",
    "typebox": "^1.3.26"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@types/node": "^24.3.0",
    "firebase": "^12.18.0",
    "firebase-tools": "^15.29.0",
    "typescript": "^7.0.2"
  }
}
