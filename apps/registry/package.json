{
  "name": "@hitslop/registry",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "check": "tsc -p tsconfig.json",
    "build": "true",
    "test": "true"
  },
  "dependencies": {
    "@hitslop/schema": "workspace:*",
    "convex": "^1.45.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
