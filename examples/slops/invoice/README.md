# Invoice

A warm editorial invoice for editing parties, terms, line items, tax, and payment status.

From the repository root:

```sh
bun slop dev examples/slops/invoice
bun slop build examples/slops/invoice
bun slop register examples/slops/invoice
```

Build a writable copy before editing. The registered template is an immutable master.
