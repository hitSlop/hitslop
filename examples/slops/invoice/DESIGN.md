# Invoice design

The invoice is warm paper with a quiet green signal, tabular totals, crisp rules,
and direct editing. The large invoice number anchors the header; parties and
dates stay easy to scan above the item ledger. Narrow windows stack party and
total groups while retaining every field. Export removes controls and presents
the same document as a clean, searchable page. Reduced motion is respected.
