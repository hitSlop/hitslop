# Cornell Notes

A classic Cornell lecture notebook page with a cue column, notes area, summary box, and an active-recall study mask.

From the repository root:

```sh
bun slop dev examples/slops/cornell-notes
bun slop build examples/slops/cornell-notes
bun slop register examples/slops/cornell-notes
```

Create a writable copy of the registered template before editing.
