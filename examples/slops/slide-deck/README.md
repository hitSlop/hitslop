# Slide Deck

A presentation studio with structured slide layouts, inline editing, and a fullscreen presenter mode.

From the repository root:

```sh
bun slop dev examples/slops/slide-deck
bun slop build examples/slops/slide-deck
bun slop register examples/slops/slide-deck
```

Create a writable copy of the registered template before editing.
