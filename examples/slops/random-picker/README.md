# Random Picker

A horizontal capsule utility for editing options, making a lively pick, and retaining short history.

Preview with `slop dev examples/slops/random-picker`. Publish with `slop publish examples/slops/random-picker`.
