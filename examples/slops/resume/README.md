# Resume

A local, direct-editing one-page resume. Profile details, summary, skills,
experience, and education persist through the JSON bridge. Editing controls are
excluded from static PNG, PDF, Quick Look, and catalog capture.

Preview with `bun slop dev examples/slops/resume`. Install with
`bun slop install examples/slops/resume`.
