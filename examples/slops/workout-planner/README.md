# Workout Planner

A dark training console for logging individual sets, following a routine, and timing rest.

From the repository root:

```sh
bun slop dev examples/slops/workout-planner
bun slop build examples/slops/workout-planner
bun slop register examples/slops/workout-planner
```

Create a writable copy of the registered template before editing.
