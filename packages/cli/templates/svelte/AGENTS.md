# hitSlop authoring

Read `manifest.json` first. Use the local `hitslop-design` skill when creating or revising the interface. Preview with `bunx @hitslop/cli dev`, then run `build` and `publish`. Development data stays in `.hitslop/dev/stores`; source, skills, dependencies, and editable styles never enter the runtime `.slop`.
