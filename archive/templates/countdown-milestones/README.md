# Countdown & Milestones

A tall capsule countdown with persistent checkpoints and an authored progress path.

Preview with `slop dev Templates/countdown-milestones`. Release with `slop package-templates`.
