# Archived templates

These authored templates are intentionally outside the active workspace. Most
need repair, accessibility work, or a stronger visual pass before they should
be migrated to `hitslop/1` and published through the registry.

The previous roadmap is retained in `ROADMAP.md` as design history. Do not ship
or package this directory automatically.
